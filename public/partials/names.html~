
<h4 class="blog-post-title">{{ tablep_name }}</h4> 
<hr/>
<button type="button" class="btn btn-danger fa fa-plus" ng-click="open({});">&nbsp;Add New {{ tablep_name }}</button>
<div class="col-md-4">
            <h5>Filtered {{ filtered.length }} of {{ totalItems}} total customers</h5>
 </div>


<div class="table-responsive">
<div class="panel panel-primary">
  <div class="panel-heading">List of {{ tablep_name }}
      <div class="sw-search" >
            <div class="nav-search" id="nav-search">
                    Filter: <span class="input-icon">
                        <input placeholder="Filter {{ tablep_name }}s list ..." class="nav-search-input" ng-model="filterTableName" ng-change="resetLimit();"  autocomplete="off" type="text" style="width:300px;" focus>
                        <i class="search-icon fa fa-search nav-search-icon"></i>
                    </span>
                
                    PageSize: <span class="input-icon">
                        <select ng-model="entryLimit" class="nav-search-input" style="width:50px;">
                            <option>5</option>
                            <option>10</option>
                            <option>20</option>
                            <option>50</option>
                            <option>100</option>
                        </select>
                    
            </div>
        </div>
    </div>
  <div class="panel-body">
    <table class="table table-striped">
    <tr ng-show="t_name[tables_name].length==0"><td style="vertical-align:middle;"><i class="fa fa-ban fa-3x"></i>&nbsp;No data found</td></tr>
    <tr ng-hide="t_name[tables_name].length>-1"><td style="vertical-align:middle;"><i class="fa fa-cog fa-3x fa-spin"></i>&nbsp;Loading</td></tr>
    
    <tr><th ng-repeat="c in columns">{{c.text}} <br><a ng-click="sort_by(c.predicate);"><i class="glyphicon glyphicon-sort"></i></a></th></tr>
  
    <tr ng-repeat="c in filtered = ( t_name[tables_name] | filter:filterTableName | orderBy :'-id' | orderBy : predicate :reverse) | startFrom:(currentPage-1)*entryLimit | limitTo:entryLimit" id="{{c.id}}" animate-on-change='c.firstname + c.middlename + c.lastname + c.age + c.gender' ng-animate=" 'animate'">

         <td ng-repeat="column in columns" ng-if="column.show !== false"> {{c[column.predicate]}}</td> 
       
        <td>
            <button class="btn" ng-class="{Active:'btn-success', Inactive:''}[c.status]" ng-click="changeStatus(c);">{{c.status}}</button>
        </td>
       
        <td style="width:100px">
            <div class="btn-group">
              <button type="button" class="btn btn-default fa fa-edit" ng-click="open(c);"></button>
              <button type="button" class="btn btn-danger fa fa-trash-o" ng-click="deleteTableName(c);"></button>
            </div>
        </td>
    </tr>
        
        
    </table>
      
         <div class="col-md-12" ng-show="filteredItems == 0">
            <div class="col-md-12">
                <h4>No {{ tablep_name }} found</h4>
            </div>
        </div>
        <div class="col-md-12" ng-show="filteredItems > 0">    
            <div pagination="" page="currentPage" on-select-page="setPage(page)" boundary-links="true" total-items="filteredItems" items-per-page="entryLimit" class="pagination-small" previous-text="&laquo;" next-text="&raquo;"></div>
            
            
        </div>
</div>
</div>
</div>
              
